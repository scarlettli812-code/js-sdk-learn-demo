{
  "name": "lark-base-scheduler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@lark-base-open/js-sdk": "^0.5.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.4.14",
    "vitest": "^2.1.8"
  }
}
